apiVersion: v1
kind: Service   
metadata:
  name: cache-service
  namespace: refundai
spec: 
  selector:
    app: cache-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30041
  type: NodePort